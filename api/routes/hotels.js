import express from "express";
const router = express.Router();


// CREATE
router.get('/', (req, res) => {
    res.send("We will create the hotel")
})

// DELETE
// UPDATE
// GET
// GET ALL
export default router;
